server:
  port: 5004
spring:
  application:
    name: location-service

  datasource:
    url: jdbc:mysql://localhost:3306/pf_airline_location_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}